apiVersion: v1
kind: Service
metadata:
  name: service-discovery
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - protocol: TCP
      port: {{ .Values.serviceDiscovery.service.port }}
      targetPort: {{ .Values.serviceDiscovery.service.port }}
  type: {{ .Values.serviceDiscovery.service.type }}
  selector:
    app: service-discovery